# 新 Session 启动提示词（拷进新会话即可开工）

**复制下面整段，粘到新开 session 的第一条消息里。**

---

你是 Token King 项目的延续 session。前一个 session 在 2026-07-06/07 闭环了 B44 bug 链（17 个 commit），现在有新需求要展开。

## 当前状态（截至 2026-07-07）
- HEAD: `c50d98c` on `main`，已 push 到 `origin/main`
- 测试: 414 pass / 19 skipped（network） / 0 fail
- Fork: `opgginc/opencode-bar`，落后 upstream 4 commits（design intent，**不 merge**）
- B44 主线 bug 全修（数据/UI/anchor recovery 3 层），Layer C 真根因待 P2 lldb
- 双屏环境验证无问题（B39/B40 低优搁置 OK）
- 未 commit 的 P3 dirty：`.gitignore` + `Info.plist GitCommitHash`（按 rule 不 commit）

## 项目入口文件（按优先级读）
1. `docs/需求池.md` — **本次新需求**，F1-F4 已写明
2. `docs/handoffs/2026-07-07-b44-session-handoff.md` — 上次 session 全链，B44 8 层根因 + 测试 + lessons
3. `docs/backlog/bugs/README.md` — 当前 backlog（B39/B40/B46/B49-B51 待处理，Layer C anchor P2）
4. `~/projects/usage-deck/AGENTS.md` — fork-specific rules + branding + design tokens + UI 规则
5. `~/projects/usage-deck/CLAUDE.md` — 信号 + 版本管理强制规则
6. `~/.claude/projects/-Users-simengyu/memory/项目/Token King.md` — 长期产品原则 + 历史背景

## 本次新需求（见 `docs/需求池.md` 全量）

4 个用户表达的需求，已经结构化：

- **F1**: Token 统计 by daily/monthly
- **F2**: 金额换算比率（API 价 vs 订阅价），例 "¥199 kimi 订阅，按当前 API 用量折算我花了多少"
- **F3**: 使用记录 — 5h 桶当天触达明细 + 本周累计百分比
- **F4**: 全局统计模块（顶层入口，跨 provider 汇总）

每条都有"用户原始描述 / 已有数据基础 / 需要决策 / 潜在拆解 / 与已有功能的依赖关系"。

## 你的下一步流程

按 **superpowers → brainstorming skill** 走：

1. 用 `superpowers:brainstorming` skill（如果装了）或 OpenCode 里 `skill tool` 加载它
2. 对 F1-F4 各自做一次 brainstorming，重点是 需求池.md 里"需要决策"部分列出的开放问题
3. 让用户拍板"哪些进 backlog / 哪些是 no" — 用户会优先 ROI 高的
4. 写 spec → 估 ROI → 走 swarm 走 subagent 实现 → 真 e2e driver test
5. 落地后按 CLAUDE.md 信号规则存档 + bump version

## 不要做的事

- 不要直接进设计 — F1-F4 都有"需要决策"的开放项，先 brainstorming
- 不要 ship 没复现的 fix（5 问自检）
- 不要"单元测试过"当 UI 修复证据 — UI bug 必须 e2e driver test
- 不要 merge upstream/main — fork 规则
- 不要把 .gitignore / Info.plist GitCommitHash 当 TODO — P3 by convention

## 工作环境
- Xcode project: `CopilotMonitor/CopilotMonitor.xcodeproj`（scheme `CopilotMonitor`）
- 关键约束: pbxproj 手动管理（见 AGENTS.md），新增 .swift 必须 4 处注册
- SwiftLint 未装 — pre-commit 检查会 warn 但不 block
- 测试命令: `xcodebuild -project CopilotMonitor/CopilotMonitor.xcodeproj -scheme CopilotMonitor -configuration Debug -derivedDataPath /tmp/tk-derived test`
- 观测文件: `/tmp/provider_debug.log`（debugLog 写入）, `/tmp/tk_observ.log`（AppDelegate observability）

---

## 上下文复习（如果新 session 丢了部分上下文）

**Token King 是什么**: fork 自 `opgginc/opencode-bar` 的 macOS 菜单栏 AI 用量监控。用户是 PM，fork 是个人学习用、非生产。

**关键坑**:
- 国内优先 — 海外/国内是两个独立 provider（不能在同一 provider 内塞多 endpoint）
- 设计原则: USD 是数据层唯一真值，国内用 cnyCost 人民币原生价
- 测试 isolation: SubscriptionSettingsManager 已有 init(defaults:) 注入，B12 时打好框架
- 现有 "额度状态" 顶部 header 是 subscription 价格统计，不是用量

**上一个 session 找到的 hidden bug**: B52 — `findLikelyDuplicateSubscriptionGroups` 按 first dot 分组 suffix，会把 `kimi/cn/codex.user@gmail.com` 全归同 TLD group。修法: `(family, suffix)` 分组。**已 fix in 7fcccb1**。

**上一个 session 自爆的 no-op fix**: Priority 1 顺序调换 (cancelTracking + updateMultiProviderMenu 换顺序)，发现其实不影响时序，自撤回。教训落库到 `~/.claude/.../memory/行为偏好/no-op fix 不能 ship.md`。

---

**口吻对用户的提醒**: 用户偏好**对话直接 + 文档清晰 + 不拍胸脯**（被 no-op fix 拍过几次胸脯反弹）。回答问题**先给结论再给证据**，不要 "let me check" 然后沉默很久。

用户是 PM 不是 dev，所以**大白话**比术语更友好。但他要思考深度，所以不要 hide trade-offs。
