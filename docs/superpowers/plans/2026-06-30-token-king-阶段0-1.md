# Token King — 阶段 0+1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 fork 的 opencode-bar 改造成 Token King：完成 fork 治理（改名/解锁/双 remote），并让 Tavily 从单 key 升级为多 key 多账号显示（数据源走 ai-infra keys.local.yaml，经 KeySource 抽象隔离）。

**Architecture:** 阶段0 是 fork 治理（改 bundle ID、app 名、AGENTS.md 解锁、README 署名）。阶段1 引入 `KeySource` 协议隔离 key 来源（第一版读 ai-infra 的 yaml），改造 `TavilySearchProvider` 用 `ProviderResult.accounts`（已内建的多账号通道）显示每个 key 的用量，照 `GeminiCLIProvider` 多账号模式。

**Tech Stack:** Swift 6.3 / Xcode 26 / XCTest / xcodebuild。工程在 `~/projects/usage-deck/CopilotMonitor/`，scheme = `CopilotMonitor`。

**关键事实（已摸清，实现时不要重新假设）：**
- 测试命令：`cd ~/projects/usage-deck/CopilotMonitor && xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -30`
- 构建命令：把 `test` 换成 `build`
- fixture 机制：测试用 `loadFixture(named:)` 读 `CopilotMonitorTests/Fixtures/<name>.json`（`Bundle(for:).url(forResource:withExtension:"json")`）
- `ProviderResult` 已有 `accounts: [ProviderAccountResult]?`，多账号无需造新结构
- `ProviderAccountResult` 字段：`accountIndex: Int / accountId: String? / usage: ProviderUsage / details: DetailedUsage?`
- Tavily 用量端点：`GET https://api.tavily.com/usage`，Bearer 鉴权；响应 `account.plan_usage / account.plan_limit / account.current_plan`
- ai-infra key 源：`~/projects/ai-infra/.private/resources/keys.local.yaml`，结构 `secrets.tavily.{apple,github,google,qq}.value` = `tvly-...`
- Gemini 多账号范本：`Providers/GeminiCLIProvider.swift`（candidates[] + dedup + map），测试范本 `CopilotMonitorTests/GeminiCLIProviderTests.swift`
- Swift 无原生 YAML 解析。keys.local.yaml 是简单缩进结构，用轻量手写解析器（只提取 tavily 段的 `name: value`），不引第三方依赖

---

## 阶段 0：Fork 治理

### Task 0.1：建 GitHub fork 并配置双 remote

**Files:** 无（git 操作）

- [ ] **Step 1：在 GitHub 网页 fork opencode-bar**

用户手动操作：浏览器打开 `https://github.com/opgginc/opencode-bar` → 点 Fork → 创建到自己账号（仓库名可填 `token-king`）。
（这步是 GUI，AI 无法代做。完成后告诉 AI 你的 fork URL。）

- [ ] **Step 2：添加 origin remote 指向用户 fork**

Run（把 `<YOUR_FORK_URL>` 换成上一步的实际 URL，如 `git@github.com:simengyu/token-king.git`）：
```bash
cd ~/projects/usage-deck
git remote add origin <YOUR_FORK_URL>
git remote -v
```
Expected: 看到 `upstream`（opgginc）和 `origin`（用户 fork）两个 remote。

- [ ] **Step 3：确认当前在 feat/fork-setup 分支**

Run:
```bash
cd ~/projects/usage-deck && git branch --show-current
```
Expected: `feat/fork-setup`（spec commit 所在分支）

### Task 0.2：改写 AGENTS.md 加定制分支声明

**Files:**
- Modify: `~/projects/usage-deck/AGENTS.md`（在文件最顶部插入）

- [ ] **Step 1：在 AGENTS.md 第 1 行前插入定制声明块**

在文件开头插入以下内容（原有内容全部保留在其后）：
```markdown
# ⚠️ FORK NOTICE — Token King (Personal Customized Build)

This is a **personal customized fork** of `opgginc/opencode-bar`, maintained for
learning purposes. The rules below from upstream are **OVERRIDDEN** in this fork:

- **UI Language**: Chinese is ALLOWED (upstream required English-only). See Phase 3 i18n.
- **Currency**: RMB + exchange-rate conversion is ALLOWED (upstream was USD-only). See Phase 2.
- **Branding**: This fork is "Token King" (bundle id `com.tokenking.app`), NOT "OpenCode Bar".

Upstream's original rules follow below and still apply EXCEPT where overridden above.
Design spec: `docs/superpowers/specs/2026-06-30-token-king-fork设计.md`

---

```

- [ ] **Step 2：提交**

Run:
```bash
cd ~/projects/usage-deck
git add AGENTS.md
git commit -m "docs: add Token King fork notice overriding upstream rules"
```

### Task 0.3：改 bundle ID 和 app 显示名

**Files:**
- Modify: `~/projects/usage-deck/CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj`（`PRODUCT_BUNDLE_IDENTIFIER`）
- 可能 Modify: `Info.plist` 里的 `CFBundleDisplayName`

- [ ] **Step 1：定位当前 bundle ID 配置**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
grep -n "PRODUCT_BUNDLE_IDENTIFIER" CopilotMonitor.xcodeproj/project.pbxproj | head
```
Expected: 看到多行 `PRODUCT_BUNDLE_IDENTIFIER = com.copilotmonitor.CopilotMonitor;`（主 app + 测试 target + CLI target 各有）

- [ ] **Step 2：把主 app 的 bundle ID 改成 com.tokenking.app**

注意：只改主 app target 的，**测试 target 的 bundle ID 通常派生自主 app（如 `.xctest`），CLI target 单独**。先看清每个出现位置属于哪个 target，主 app 的改为 `com.tokenking.app`。测试 target 若是 `com.copilotmonitor.CopilotMonitorTests` 一并改为 `com.tokenking.app.tests` 保持一致。

用 Edit 工具逐个改（不要 sed 全局替换，避免误伤）。

- [ ] **Step 3：改 app 显示名**

Run 找到 Info.plist：
```bash
cd ~/projects/usage-deck/CopilotMonitor
find . -name "Info.plist" -not -path "*/Pods/*" | head
grep -rn "CFBundleDisplayName\|CFBundleName" CopilotMonitor/Info.plist 2>/dev/null
```
若有 `CFBundleDisplayName`，改其值为 `Token King`；若无则在 Info.plist 加 `<key>CFBundleDisplayName</key><string>Token King</string>`。
（注意：菜单栏 app 显示名还可能硬编码在 Swift 里的 "OpenCode Bar" 短标题，阶段3 中文化时统一处理，这里只改 bundle 层。）

- [ ] **Step 4：构建验证改名没破坏工程**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild build -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -15
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 5：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj CopilotMonitor/CopilotMonitor/Info.plist
git commit -m "chore: rebrand bundle id and display name to Token King"
```

### Task 0.4：README 加 fork 署名

**Files:**
- Modify: `~/projects/usage-deck/README.md`（顶部插入）

- [ ] **Step 1：README 顶部插入 fork 声明**

在 README.md 第 1 行前插入：
```markdown
> **Token King** — a personal, learning-purpose fork of
> [opgginc/opencode-bar](https://github.com/opgginc/opencode-bar) (MIT License).
> All credit for the original work goes to OP.GG. This fork adds multi-key,
> multi-engine, RMB currency, Chinese UI, and a desktop widget for personal use.

---

```

- [ ] **Step 2：提交**

Run:
```bash
cd ~/projects/usage-deck
git add README.md
git commit -m "docs: add fork attribution to README"
```

### Task 0.5：阶段0 验收

- [ ] **Step 1：确认能编译能跑、与官方版不冲突**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild build -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -5
# 确认 bundle id 已改
grep -c "com.tokenking.app" CopilotMonitor.xcodeproj/project.pbxproj
```
Expected: BUILD SUCCEEDED；grep 计数 ≥1（bundle id 已是新值，与 brew 装的 `com.copilotmonitor.CopilotMonitor` 不冲突）

- [ ] **Step 2：推送到 fork（首次）**

Run:
```bash
cd ~/projects/usage-deck
git push -u origin feat/fork-setup
```
Expected: 推送成功，GitHub 上能看到分支。

---

## 阶段 1：多 Tavily Key + 多搜索引擎

> 先做 KeySource 抽象 → 改 Tavily 多 key → （anysearch 单独，依赖其 API 调研）

### Task 1.1：定义 KeySource 协议 + ai-infra yaml 实现

**Files:**
- Create: `~/projects/usage-deck/CopilotMonitor/CopilotMonitor/Services/KeySource.swift`
- Create: `~/projects/usage-deck/CopilotMonitor/CopilotMonitorTests/KeySourceTests.swift`
- Create: `~/projects/usage-deck/CopilotMonitor/CopilotMonitorTests/Fixtures/ai_infra_keys.yaml`

- [ ] **Step 1：写 fixture（模拟 ai-infra keys.local.yaml 的 tavily 段）**

Create `CopilotMonitorTests/Fixtures/ai_infra_keys.yaml`:
```yaml
secrets:
  tavily:
    apple:
      env_var: TAVILY_API_KEY
      value: tvly-dev-APPLE0000000000000000000000000000
    github:
      env_var: TAVILY_API_KEY
      value: tvly-dev-GITHUB000000000000000000000000000
    google:
      env_var: TAVILY_API_KEY
      value: tvly-dev-GOOGLE000000000000000000000000000
    qq:
      env_var: TAVILY_API_KEY
      value: tvly-dev-QQ0000000000000000000000000000000
  brave:
    default:
      value: BSA000000000000000000000000000
```

- [ ] **Step 2：写失败测试**

Create `CopilotMonitorTests/KeySourceTests.swift`:
```swift
import XCTest
@testable import OpenCode_Bar

final class KeySourceTests: XCTestCase {
    private func fixtureURL(_ name: String, ext: String) -> URL {
        let bundle = Bundle(for: type(of: self))
        return bundle.url(forResource: name, withExtension: ext)!
    }

    func testYamlKeySourceParsesAllTavilyKeys() throws {
        let url = fixtureURL("ai_infra_keys", ext: "yaml")
        let source = AIInfraYamlKeySource(fileURL: url)
        let keys = try source.keys(forProvider: "tavily")

        XCTAssertEqual(keys.count, 4)
        let names = Set(keys.map { $0.name })
        XCTAssertEqual(names, ["apple", "github", "google", "qq"])
        let appleKey = keys.first { $0.name == "apple" }
        XCTAssertEqual(appleKey?.value, "tvly-dev-APPLE0000000000000000000000000000")
    }

    func testYamlKeySourceReturnsEmptyForUnknownProvider() throws {
        let url = fixtureURL("ai_infra_keys", ext: "yaml")
        let source = AIInfraYamlKeySource(fileURL: url)
        let keys = try source.keys(forProvider: "nonexistent")
        XCTAssertTrue(keys.isEmpty)
    }
}
```

- [ ] **Step 3：跑测试确认失败**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/KeySourceTests 2>&1 | tail -20
```
Expected: 编译失败（`AIInfraYamlKeySource` 未定义）

- [ ] **Step 4：实现 KeySource.swift**

Create `CopilotMonitor/Services/KeySource.swift`:
```swift
import Foundation

/// One named API key for a provider (e.g. tavily/apple).
struct NamedKey: Equatable {
    let name: String
    let value: String
}

/// Abstraction over where provider API keys come from.
/// First implementation reads ai-infra's keys.local.yaml; can be swapped later
/// for an `ai-infra keys export` command without touching consumers.
protocol KeySource {
    func keys(forProvider provider: String) throws -> [NamedKey]
}

/// Reads keys from ai-infra's private keys.local.yaml.
/// Structure: secrets.<provider>.<name>.value
/// Lightweight indentation parser — only extracts the requested provider's
/// `name: value` pairs. No third-party YAML dependency.
final class AIInfraYamlKeySource: KeySource {
    private let fileURL: URL

    init(fileURL: URL = AIInfraYamlKeySource.defaultURL()) {
        self.fileURL = fileURL
    }

    static func defaultURL() -> URL {
        let home = URL(fileURLWithPath: NSHomeDirectory())
        return home.appendingPathComponent("projects/ai-infra/.private/resources/keys.local.yaml")
    }

    func keys(forProvider provider: String) throws -> [NamedKey] {
        let content = try String(contentsOf: fileURL, encoding: .utf8)
        let lines = content.components(separatedBy: .newlines)

        // Find indentation depth of `secrets:` children, then locate `<provider>:`
        var inSecrets = false
        var providerIndent: Int?
        var inProvider = false
        var currentKeyName: String?
        var result: [NamedKey] = []

        func indent(of line: String) -> Int {
            line.prefix { $0 == " " }.count
        }

        for rawLine in lines {
            let line = rawLine
            let trimmed = line.trimmingCharacters(in: .whitespaces)
            if trimmed.isEmpty || trimmed.hasPrefix("#") { continue }
            let lineIndent = indent(of: line)

            if trimmed == "secrets:" {
                inSecrets = true
                continue
            }
            guard inSecrets else { continue }

            if trimmed == "\(provider):" {
                providerIndent = lineIndent
                inProvider = true
                currentKeyName = nil
                continue
            }

            if inProvider {
                guard let pIndent = providerIndent else { continue }
                // A sibling of provider (same or less indent, ends with ':') exits provider scope
                if lineIndent <= pIndent && trimmed.hasSuffix(":") {
                    inProvider = false
                    continue
                }
                // Key name line: e.g. "apple:" indented deeper than provider
                if trimmed.hasSuffix(":") && lineIndent == pIndent + 2 {
                    currentKeyName = String(trimmed.dropLast())
                    continue
                }
                // Value line: "value: tvly-..."
                if trimmed.hasPrefix("value:"), let name = currentKeyName {
                    let value = trimmed
                        .replacingOccurrences(of: "value:", with: "")
                        .trimmingCharacters(in: .whitespaces)
                    if !value.isEmpty {
                        result.append(NamedKey(name: name, value: value))
                    }
                }
            }
        }
        return result
    }
}
```

- [ ] **Step 5：把新文件加入 Xcode 工程并跑测试**

新建的 `.swift`/fixture 需加入 target。用 Xcode GUI 拖入，或确认 project.pbxproj 已包含（subagent 执行时若用命令行，需 `xcodebuild` 能发现）。然后：
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/KeySourceTests 2>&1 | tail -20
```
Expected: 两个测试 PASS。
（注意：fixture .yaml 必须加入测试 target 的 Copy Bundle Resources，否则 `url(forResource:)` 返回 nil。）

- [ ] **Step 6：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Services/KeySource.swift CopilotMonitor/CopilotMonitorTests/KeySourceTests.swift CopilotMonitor/CopilotMonitorTests/Fixtures/ai_infra_keys.yaml CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj
git commit -m "feat: add KeySource abstraction with ai-infra yaml implementation"
```

### Task 1.2：抽出 Tavily 单 key 用量解析为可测函数

**Files:**
- Modify: `CopilotMonitor/Providers/TavilySearchProvider.swift`
- Create: `CopilotMonitorTests/TavilySearchProviderTests.swift`
- Create: `CopilotMonitorTests/Fixtures/tavily_usage_response.json`

> 当前 fetch() 把"拿 key + 请求 + 解析"耦在一起，无法单测。先把"解析响应 → ProviderUsage+DetailedUsage"抽成纯函数，加测试覆盖，再在 1.3 加多 key 循环。

- [ ] **Step 1：写 fixture**

Create `CopilotMonitorTests/Fixtures/tavily_usage_response.json`:
```json
{
  "account": {
    "current_plan": "Researcher",
    "plan_usage": 67,
    "plan_limit": 1000
  },
  "key": { "usage": 67, "limit": null }
}
```

- [ ] **Step 2：写失败测试**

Create `CopilotMonitorTests/TavilySearchProviderTests.swift`:
```swift
import XCTest
@testable import OpenCode_Bar

final class TavilySearchProviderTests: XCTestCase {
    func testParseUsageFromResponseData() throws {
        let bundle = Bundle(for: type(of: self))
        let url = bundle.url(forResource: "tavily_usage_response", withExtension: "json")!
        let data = try Data(contentsOf: url)

        let parsed = try TavilySearchProvider.parseUsage(from: data, keyName: "google")

        XCTAssertEqual(parsed.used, 67)
        XCTAssertEqual(parsed.limit, 1000)
        XCTAssertEqual(parsed.remaining, 933)
        XCTAssertEqual(parsed.planName, "Researcher")
        if case .quotaBased(let remaining, let entitlement, _) = parsed.usage {
            XCTAssertEqual(remaining, 933)
            XCTAssertEqual(entitlement, 1000)
        } else {
            XCTFail("expected quotaBased usage")
        }
    }
}
```

- [ ] **Step 3：跑测试确认失败**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/TavilySearchProviderTests 2>&1 | tail -20
```
Expected: 编译失败（`parseUsage` / `TavilyParsedUsage` 未定义）

- [ ] **Step 4：在 TavilySearchProvider 加可测的解析结构和静态方法**

在 `TavilySearchProvider.swift` 的 `final class TavilySearchProvider` 内部加（不删原 fetch，下一步再接线）：
```swift
    struct TavilyParsedUsage {
        let keyName: String
        let used: Int
        let limit: Int
        let remaining: Int
        let planName: String?
        let usage: ProviderUsage
    }

    static func parseUsage(from data: Data, keyName: String) throws -> TavilyParsedUsage {
        let decoded: TavilyUsageResponse
        do {
            decoded = try JSONDecoder().decode(TavilyUsageResponse.self, from: data)
        } catch {
            throw ProviderError.decodingError("Invalid Tavily usage response")
        }
        let used = decoded.account?.planUsage ?? decoded.account?.paygoUsage ?? decoded.key?.usage
        let limit = decoded.account?.planLimit ?? decoded.account?.paygoLimit ?? decoded.key?.limit
        guard let resolvedUsed = used, let resolvedLimit = limit, resolvedLimit > 0 else {
            throw ProviderError.decodingError("Missing Tavily usage or limit")
        }
        let remaining = max(0, resolvedLimit - resolvedUsed)
        let usage = ProviderUsage.quotaBased(remaining: remaining, entitlement: resolvedLimit, overagePermitted: false)
        return TavilyParsedUsage(
            keyName: keyName,
            used: resolvedUsed,
            limit: resolvedLimit,
            remaining: remaining,
            planName: decoded.account?.currentPlan,
            usage: usage
        )
    }
```
注意：`TavilyUsageResponse` 当前是文件私有 `private struct`。把它的 `private` 去掉（改成 fileprivate 不够，因为静态方法在类内但 struct 在类外文件作用域——它们同文件，`private` 在同文件类型间可见，保持 `private struct` 即可，static 方法在同文件内能访问）。**验证：编译报错再调整可见性。**

- [ ] **Step 5：跑测试确认通过**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/TavilySearchProviderTests 2>&1 | tail -20
```
Expected: PASS（fixture json 需加入测试 target Copy Bundle Resources）

- [ ] **Step 6：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Providers/TavilySearchProvider.swift CopilotMonitor/CopilotMonitorTests/TavilySearchProviderTests.swift CopilotMonitor/CopilotMonitorTests/Fixtures/tavily_usage_response.json CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj
git commit -m "refactor: extract testable Tavily usage parser"
```

### Task 1.3：Tavily fetch() 改多 key，用 accounts 显示

**Files:**
- Modify: `CopilotMonitor/Providers/TavilySearchProvider.swift`（init 注入 KeySource，重写 fetch）

- [ ] **Step 1：改 init 注入 KeySource**

把 `TavilySearchProvider` 的 init 改为：
```swift
    private let keySource: KeySource

    init(tokenManager: TokenManager = .shared,
         session: URLSession = .shared,
         keySource: KeySource = AIInfraYamlKeySource()) {
        self.tokenManager = tokenManager
        self.session = session
        self.keySource = keySource
    }
```

- [ ] **Step 2：重写 fetch() 循环查所有 key**

把整个 `func fetch()` 替换为以下逻辑（保留 `formatEstimatedMonthlyResetText`）：
```swift
    func fetch() async throws -> ProviderResult {
        let namedKeys = (try? keySource.keys(forProvider: "tavily")) ?? []

        // Fallback: if ai-infra source empty, use the single key from TokenManager
        var keysToQuery: [NamedKey] = namedKeys
        if keysToQuery.isEmpty, let single = tokenManager.getTavilyAPIKeyWithSource() {
            keysToQuery = [NamedKey(name: "default", value: single.key)]
        }
        guard !keysToQuery.isEmpty else {
            throw ProviderError.authenticationFailed("No Tavily API key available")
        }

        var accountResults: [ProviderAccountResult] = []
        var firstUsage: ProviderUsage?
        var firstDetails: DetailedUsage?

        for (index, namedKey) in keysToQuery.enumerated() {
            do {
                let parsed = try await fetchOne(key: namedKey)
                let resetText = formatEstimatedMonthlyResetText()
                let details = DetailedUsage(
                    monthlyUsage: Double(parsed.used),
                    limit: Double(parsed.limit),
                    limitRemaining: Double(parsed.remaining),
                    resetPeriod: resetText,
                    authSource: "ai-infra:tavily.\(namedKey.name)",
                    authUsageSummary: parsed.planName ?? "Auto refresh",
                    mcpUsagePercent: normalizedTavilyQuotaUsagePercent(used: parsed.used, limit: parsed.limit)
                )
                accountResults.append(ProviderAccountResult(
                    accountIndex: index,
                    accountId: namedKey.name,
                    usage: parsed.usage,
                    details: details
                ))
                if firstUsage == nil {
                    firstUsage = parsed.usage
                    firstDetails = details
                }
                tavilyLogger.info("Tavily[\(namedKey.name)]: used=\(parsed.used)/\(parsed.limit)")
            } catch {
                tavilyLogger.error("Tavily[\(namedKey.name)] fetch failed: \(error.localizedDescription)")
                // partial success: skip this key, continue others
            }
        }

        guard let topUsage = firstUsage else {
            throw ProviderError.networkError("All Tavily keys failed")
        }
        return ProviderResult(usage: topUsage, details: firstDetails, accounts: accountResults)
    }

    private func fetchOne(key namedKey: NamedKey) async throws -> TavilyParsedUsage {
        guard let url = URL(string: "https://api.tavily.com/usage") else {
            throw ProviderError.networkError("Invalid Tavily usage endpoint")
        }
        var request = URLRequest(url: url)
        request.httpMethod = "GET"
        let apiKey = namedKey.value
        let authValue = apiKey.lowercased().hasPrefix("bearer ") ? apiKey : "Bearer \(apiKey)"
        request.setValue(authValue, forHTTPHeaderField: "Authorization")
        request.setValue("application/json", forHTTPHeaderField: "Accept")

        let (data, response) = try await session.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ProviderError.networkError("Invalid Tavily response type")
        }
        if httpResponse.statusCode == 401 || httpResponse.statusCode == 403 {
            throw ProviderError.authenticationFailed("Invalid Tavily API key")
        }
        guard (200...299).contains(httpResponse.statusCode) else {
            throw ProviderError.networkError("HTTP \(httpResponse.statusCode)")
        }
        return try Self.parseUsage(from: data, keyName: namedKey.name)
    }
```

- [ ] **Step 2.5：检查多账号菜单渲染是否需要 Tavily 专门分支**

Run（看 StatusBarController 怎么渲染 accounts，确认 Tavily 的 accounts 能被显示）：
```bash
cd ~/projects/usage-deck/CopilotMonitor
grep -rn "result.accounts\|\.accounts" CopilotMonitor/Menu/ CopilotMonitor/Helpers/ 2>/dev/null | head
```
Expected: 找到通用渲染 accounts 的代码路径。若只有 Gemini 专门分支（`identifier == .geminiCLI`），需在同处加 `|| identifier == .tavilySearch` 或抽通用逻辑。**实现时按实际渲染代码决定，这一步是 discovery，不是盲改。**

- [ ] **Step 3：构建 + 跑全量测试**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -30
```
Expected: 全部测试 PASS（含 1.1/1.2 新增的，且无回归）

- [ ] **Step 4：真机手动验证（关键 — 单测覆盖不了真实显示）**

Run 构建并启动，看菜单栏 Tavily 是否显示 4 个 key：
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild build -scheme CopilotMonitor -configuration Debug -derivedDataPath /tmp/tk-build -destination 'platform=macOS' 2>&1 | tail -5
# 杀掉旧的官方版避免混淆，启动新构建
pkill -f "OpenCode Bar" 2>/dev/null; pkill -f "Token King" 2>/dev/null
open /tmp/tk-build/Build/Products/Debug/*.app
```
Expected: 菜单栏出现图标，Tavily 项展开后能看到 apple/github/google/qq 四个 key 各自用量。**这一步必须人工看菜单确认，不能只靠测试。** 若空白，看 Console `log stream --predicate 'subsystem == "com.opencodeproviders"'`。

- [ ] **Step 5：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Providers/TavilySearchProvider.swift
git commit -m "feat: Tavily multi-key support via ai-infra KeySource"
```

### Task 1.4：anysearch provider（依赖 API 调研，先调研再实现）

**Files:**
- Create: `CopilotMonitor/Providers/AnySearchProvider.swift`（待 API 确认后）

- [ ] **Step 1：调研 anysearch 有无用量 API**

先确认 anysearch 的 base URL（已知 opencode.json 里 `anysearch` MCP = `https://api.anysearch.com/mcp`，Bearer `as_sk_...`）。用 curl 探用量端点：
```bash
AS_KEY=$(python3 -c "import json;d=json.load(open('$HOME/.config/opencode/opencode.json'));print(d['mcp']['anysearch']['headers']['Authorization'].replace('Bearer ',''))")
for ep in "https://api.anysearch.com/usage" "https://api.anysearch.com/v1/usage" "https://api.anysearch.com/account" "https://api.anysearch.com/v1/me"; do
  echo "-- $ep"; curl -s -m 6 -H "Authorization: Bearer $AS_KEY" "$ep" | head -c 200; echo
done
```
Expected: 找到返回用量/余额的端点。**若全部 404/无用量 API → anysearch 这条暂时搁置，记入 spec 残留项，不强行实现。**

- [ ] **Step 2（仅当 Step 1 找到 API）：照 TavilySearchProvider 模式实现 AnySearchProvider**

参考 `TavilySearchProvider`（含 `parseUsage` 静态方法 + 测试 + fixture），在 `ProviderIdentifier` enum 加 `case anySearch = "anysearch"` 和 displayName，新建 provider 文件 + 测试 + fixture，注册到 ProviderManager。具体字段按 Step 1 实测的响应结构定。

- [ ] **Step 3：提交（若实现了）**

```bash
cd ~/projects/usage-deck
git add -A
git commit -m "feat: add AnySearch provider"
```

### Task 1.5：阶段1 验收

- [ ] **Step 1：全量测试通过**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -10
```
Expected: 全部 PASS

- [ ] **Step 2：逐项对照验收**
  - [ ] KeySource 抽象存在，能从 ai-infra yaml 读出 4 个 tavily key（KeySourceTests PASS）
  - [ ] Tavily 解析逻辑有单测覆盖（TavilySearchProviderTests PASS）
  - [ ] 菜单栏 Tavily 实际显示 4 个 key 各自用量（Task 1.3 Step 4 人工确认）
  - [ ] anysearch：要么实现并显示，要么已确认无 API 并记入残留项
  - [ ] 无回归（其他 provider 测试仍 PASS）

- [ ] **Step 3：推送**

Run:
```bash
cd ~/projects/usage-deck
git push origin feat/fork-setup
```

---

## 残留 / 后续阶段（不在本计划详细展开）

- 阶段2（货币 RMB+汇率 / 菜单精简）、阶段3（中文化）、阶段4（widget）：待阶段0+1 完成、摸熟 codebase 后再写详细计划。
- anysearch 若 Step 1.4 确认无用量 API，记此处待 API 出现。
